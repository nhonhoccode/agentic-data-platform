name: olist_ai_platform
version: 1.0.0
profile: olist_ai_platform

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  olist_ai_platform:
    +materialized: view
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
    serving:
      +schema: serving
      +materialized: view
